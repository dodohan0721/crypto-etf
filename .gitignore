# ⚠️ 절대 커밋되면 안 되는 것
config.py
_secrets_등록용.txt
.env
*.key
*.pem
.cache/kis_token.json

# 캐시·임시파일 — 기본은 전부 제외하고, 남겨야 할 것만 아래에서 되살린다
.cache/*
__pycache__/
*.pyc
*.log
*.tar.gz
_debug_*.html
_probe_*.json

# 순유입 계산은 "어제 상장주식수"가 있어야 한다. 이게 사라지면 하루치 흐름이 통째로 빈다.
!.cache/shares/
!.cache/prices.json
!.cache/prdt_type.json

# 속보 일별 캐시 — 차트의 날짜 버튼이 여기서 나온다. Actions 캐시는 일주일이면 지워진다.
!.cache/news/
